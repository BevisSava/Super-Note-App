version: '3.8'

services:
  # 1. DATABASE (MySQL)
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: note_app
    ports:
      - "3306:3306"
    volumes:
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql

  # 2. BACKEND (PHP + Apache)
  backend:
    build:
      context: ./backend
    restart: always
    ports:
      - "8080:80"  
    volumes:
      - ./backend:/var/www/html
    depends_on:
      - db

  # WEBSOCKET SERVER (Ratchet)
  websocket:
    build:
      context: ./backend
    restart: always
    ports:
      - "8081:8081"
    volumes:
      - ./backend:/var/www/html
    command: php socket_server.php
    depends_on:
      - db

  # 3. FRONTEND (ReactJS)
  frontend:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - ./frontend:/app
    ports:
      - "80:5173" 
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    depends_on:
      - backend